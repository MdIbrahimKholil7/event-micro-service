# Local-only overrides for running order-service outside Docker
ORDER_DB_HOST=localhost
ORDER_DB_PORT=5435
ORDER_DB_NAME=order_db
ORDER_DB_USER=order_user
ORDER_DB_PASSWORD=order_pass
KAFKA_BROKER=localhost:29092
